# Database Configuration
POSTGRES_USER=admin
POSTGRES_PASSWORD=change_me
POSTGRES_DB=mf_tracker

# Application Configuration
# Note: In docker-compose, the host for the database is the service name 'postgres'
DATABASE_URL=postgres://admin:change_me@postgres:5432/mf_tracker

# Sync Service
BACKFILL_LIMIT=50
