{
  "name": "@mf-tracker/db",
  "version": "1.0.0",
  "main": "index.ts",
  "scripts": {
    "generate": "drizzle-kit generate:pg",
    "migrate": "bun run migrate.ts"
  },
  "dependencies": {
    "drizzle-orm": "^0.30.0",
    "postgres": "^3.4.0"
  },
  "devDependencies": {
    "drizzle-kit": "^0.20.0"
  }
}
